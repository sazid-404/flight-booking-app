import React from "react";
import List from "./List";
import Options from "./Options";

const Content = () => {
  return (
    <section>
      <Options />
      <List />
    </section>
  );
};

export default Content;
